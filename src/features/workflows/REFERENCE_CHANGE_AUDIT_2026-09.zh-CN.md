# 参照项目前端最新工作流变更审阅

> 审阅日期：2026-09-14  
> 参照分支：`origin/release-jdk21-001`  
> 提交范围：`f01994d6..7e03841f`

## 提交事实

参照仓库没有提交日期位于 2026 年 9 月的工作流提交。远端最新一批为：

- `a5c7767a`（2026-08-25）：新增工作流调用、HTTP 插件调用、MCP 调用。
- `2e46506b`（2026-08-28）：升级开始变量、模板转换、代码执行、HTTP、LLM 配置及调试变量台账。
- `7e03841f`（2026-08-31）：发布逻辑调整，工作流目录没有节点协议修改。

此前只检查到 `f01994d6`（2026-08-21），因此漏掉了 8 月 25 日和 28 日两批变更。

## 逐节点结论

| 节点/能力 | 参照最新修改 | 是否影响协议 | 对齐状态 |
| --- | --- | --- | --- |
| 开始 | `file` 改为文件数组；支持默认文件上传；调试变量传 `FileInfo[]` | 是 | 已对齐（文件数组标识、统一上传校验及调试多行输入已补齐） |
| LLM / 对话 | 系统提示词和用户提示词增加 AI 优化入口；用户提示词改为多行模板编辑 | 部分 | 已对齐 |
| HTTP | URL 可插入变量；JSONPath 统一存相对路径并去掉 `$.` | 是 | 已对齐 |
| 指定回复 | 无修改 | 否 | 无需调整 |
| 文本拼接 | 原位升级为 Jinja2 模板转换；新增具名动态输入、模板示例与校验；持久化类型改为 `templateTransform` | 是，重大 | 已对齐 |
| 判断器 | 无修改 | 否 | 无需调整 |
| 变量更新 | 无修改 | 否 | 无需调整 |
| 意图识别 | 无修改 | 否 | 无需调整 |
| 文件解析 | 无修改 | 否 | 无需调整 |
| 知识库搜索 | 参数弹窗改用 Portal，修复遮挡层级 | 否 | 已对齐（项目 Dialog 底层已使用 Radix Portal） |
| 代码执行 | 仅保留 JS；默认输入输出为空；保存时保留换行；输出改为字段名与 JSONPath；代码示例与 AI 优化 | 是，重大 | 已对齐（示例与 AI 输入输出上下文已按参照补齐） |
| 数据库 | 无修改 | 否 | 无需调整 |
| 基础图表 | 无修改 | 否 | 无需调整 |
| 循环体及内部节点 | 无修改 | 否 | 无需调整 |
| 结束 | 无修改 | 否 | 无需调整 |
| 隐藏系统配置 | 新增长期记忆 | 是 | 不作为可拖拽节点，另行处理 |
| 调试变量台账 | 新增全局/节点变量、运行样本及类型不一致查看 | 调试能力 | 独立评估 |
| 工作流调用、HTTP 插件调用、MCP 调用 | 新增三个节点 | 是 | 属新增范围，不能混入既有节点修改 |

## 对齐顺序

1. 开始节点文件数组。
2. 模板转换。
3. 代码执行。
4. HTTP。
5. LLM 提示词。
6. 知识库搜索弹窗层级。
